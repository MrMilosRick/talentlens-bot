TalentLens Bot — Telegram-бот для HR-скрининга LLM-ориентированных разработчиков

TalentLens — это MVP-инструмент для первичного технического скрининга кандидатов.
Бот проводит короткий диалог (3–5 минут), структурирует ответы через LLM и сохраняет результат в Google Sheets.

Процесс:
	1.	Кандидат отвечает на 6 вопросов + отдельный шаг с пруфами (всего 7 шагов).
	2.	Ответы отправляются в LLM.
	3.	Модель возвращает строго валидный JSON (3 критерия + общий балл + summary + hot).
	4.	Результат записывается одной строкой в Google Sheet.
	5.	При высоком результате отправляется алерт в Telegram-чат (ADMIN_ALERT_CHAT_ID).

⸻

Режимы работы (бонус из ТЗ)

Кандидатский режим

Команда /start запускает прохождение скрининга.

Админский режим

Команда /admin и /admin top позволяют:
	•	посмотреть статистику,
	•	средний балл,
	•	топ-3 кандидатов,
	•	количество LLM-ошибок.

⸻

Команды бота
	•	/start — начать скрининг
	•	/restart — начать заново
	•	/cancel — остановить

⸻

Быстрый старт (локально)

Требования
	•	Python 3.11+
	•	Telegram Bot Token
	•	OpenAI API Key
	•	Google Sheet
	•	Service Account credentials (creds.json)

⸻

1) Установка

python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt


⸻

2) Конфигурация

cp .env.example .env

Пример .env:

BOT_TOKEN=123456789:replace_me
ADMIN_USER_ID=123456789
ADMIN_ALERT_CHAT_ID=-1001234567890
LLM_MODEL=gpt-5-mini
OPENAI_API_KEY=replace_me

GOOGLE_SHEETS_ID=replace_me
GOOGLE_SHEETS_WORKSHEET=Sheet1
GOOGLE_CREDS_PATH=./creds.json


⸻

3) Запуск

python main.py

После запуска бот переходит в polling-режим и готов к прохождению скрининга.

⸻

Google Sheets (обязательно для DoD)

Каждое прохождение записывается одной строкой.
Если шапки нет — бот создаёт её автоматически.

Колонки
	1.	timestamp_utc_iso
	2.	tg_user_id
	3.	username
	4.	full_name
	5.	answers_json
	6.	project_link (https://..., declined, nda)
	7.	project_note
	8.	scores_json
	9.	overall_score
	10.	hot
	11.	llm_model
	12.	latency_ms
	13.	scoring_failed
	14.	error

Поле hot (TRUE/FALSE) — техническая метка.
В интерфейсе и Telegram-алерте оно отображается как 🔥 Топ-кандидат.

⸻

Настройка Service Account
	1.	Создать проект в Google Cloud
	2.	Включить:
	•	Google Sheets API
	•	Google Drive API
	3.	IAM → Service Accounts → Create
	4.	Создать JSON-ключ
	5.	Сохранить как ./creds.json
	6.	Добавить client_email из creds.json в Google Sheet с правами Editor

⸻

AI-скоринг: логика работы

LLM оценивает ответы по 3 критериям (каждый 0–10) и возвращает строго структурированный JSON:
	•	criteria (3 элемента)
	•	overall_score_0_10
	•	hot
	•	summary_1_2_lines

Правила:

overall_score_0_10 = round(mean(criteria scores))
hot = (overall >= 8) OR (минимум 2 критерия >= 8)

Если JSON невалидный:
	•	1 автоматический retry с требованием “return valid JSON only”
	•	при повторной ошибке scoring_failed = true
	•	запись в таблицу сохраняется
	•	отправляется alert администратору

Таким образом обеспечивается воспроизводимость и устойчивость скоринга.

⸻

Сценарий скринкаста (3–5 минут)
	1.	Показать .env (без токенов)
	2.	Запустить python main.py
	3.	Пройти скрининг через /start
	4.	Показать валидацию:
	•	ввести github.com → бот попросит http/https
	•	ввести текст NDA → бот примет
	5.	Дождаться скоринга
	6.	Показать строку в Google Sheet
	7.	Показать Telegram-алерт (если кандидат hot)

⸻

Методология скрининга

Цель — за 10 минут выявить LLM-инженерное мышление, а не просто умение пользоваться ChatGPT.

1. Инженерная гигиена (Q1–Q2)

Проверяем:
	•	системность использования LLM,
	•	структурированный ввод/вывод,
	•	JSON-mode, Pydantic,
	•	контроль галлюцинаций.

2. Работа с ошибками (Q3)

LLM вероятностны.
Опытный инженер:
	•	использует guardrails,
	•	логирует ответы,
	•	строит fallback-механику.

3. Метрики вместо субъективности (Q4)

Ищем:
	•	конкретные сигналы качества,
	•	feedback-loops,
	•	LLM-as-a-judge,
	•	продуктовые метрики.

4. MVP-мышление (Q5)

Проверяем:
	•	приоритизацию,
	•	способность быстро доставлять ценность,
	•	понимание компромиссов.

5. Инфраструктурная зрелость (Q6)

Если облако запрещено:
	•	open-source модели,
	•	self-host,
	•	RAG,
	•	векторные БД,
	•	изоляция данных.

6. Артефакты (Q7)

Ссылка, NDA-описание или отказ.
Важно не только “что сделано”, но и как человек объясняет архитектуру.

⸻

Безопасность

В репозиторий не коммитятся:
	•	.env
	•	creds.json
	•	API-ключи

Сохраняются только минимальные данные для скрининга.

⸻

Соответствие ТЗ

✔ Работающий Telegram-бот
✔ Осмысленные вопросы
✔ Скоринг с объяснением
✔ Google Sheet
✔ Алерты о топ-кандидатах
✔ Админский режим
✔ Письменная методология
✔ Скринкаст 3–5 минут

⸻

TalentLens переводит субъективные ответы в измеримую и воспроизводимую систему оценки, используя LLM как механизм стандартизации, а не как “магический чёрный ящик”.
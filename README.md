🎯 TalentLens Bot

TalentLens — это инструмент для автоматического скрининга AI-first разработчиков. Бот заменяет первое техническое интервью, выявляя инженерную зрелость и глубину понимания LLM-технологий за 10 минут.

Почему это работает?

Мы используем ИИ не как непредсказуемый инструмент, а как надежный механизм оценки. Бот переводит ответы кандидата в конкретные данные, анализируя их по единой шкале. Это позволяет полностью исключить субъективность и человеческий фактор на первом этапе отбора.

🚀 Основные возможности

Умное интервью: 7 шагов (6 вопросов по существу + пруфы), направленных на выявление реального опыта, а не знания теории.

LLM-аналитика: Автоматический скоринг ответов по трем критериям с подробным текстовым обоснованием.

Прозрачность: Все результаты мгновенно сохраняются в Google Sheets — единый источник истины для HR и техлидов.

Мгновенные алерты: Если бот находит сильного («Hot») кандидата, команда тут же получает уведомление в Telegram.

Админ-панель: Встроенная статистика, просмотр топ-кандидатов и мониторинг работы системы.

🛠 Быстрый старт
Требования

Python 3.11+

OpenAI API Key

Google Cloud Service Account (файл creds.json)

1. Установка
code
Bash
download
content_copy
expand_less
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
2. Настройка

Создайте файл .env на основе .env.example:

code
Env
download
content_copy
expand_less
BOT_TOKEN=ваш_токен
ADMIN_USER_ID=ваш_id
ADMIN_ALERT_CHAT_ID=id_чата_для_уведомлений
LLM_MODEL=gpt-4o-mini
OPENAI_API_KEY=ваш_ключ

GOOGLE_SHEETS_ID=id_таблицы
GOOGLE_CREDS_PATH=./creds.json
3. Запуск
code
Bash
download
content_copy
expand_less
python main.py
🧠 Методология: Как мы ищем лучших

Мы оцениваем кандидатов по четырем ключевым столпам AI-разработки:

Инженерная гигиена: Умение работать со структурированными данными (JSON-mode, Pydantic) и понимание того, как направлять модель для решения конкретных задач.

Управление неопределенностью: Знание того, что ответы LLM вероятностны. Мы ищем тех, кто умеет строить защитные механизмы (guardrails) и обрабатывать ошибки нейросети.

Объективность: Уход от оценки «на глаз» в сторону конкретных сигналов качества и циклов обратной связи (LLM-as-a-judge, бенчмарки).

Прагматичность (MVP-мышление): Способность быстро доставить ценность, понимая компромиссы между мощными облачными моделями и быстрыми локальными решениями.

📊 Инфраструктура данных
Google Sheets

Бот автоматически создает и заполняет таблицу. Каждая строка — это полный цифровой профиль кандидата:

Контактные данные: TG ID, имя, ссылка на профиль.

Сырые ответы: Полный лог интервью.

Аналитика: Оценки по навыкам, общий балл и резюме от нейросети.

Техданные: Время ответа модели (latency) и флаг «Hot-кандидат».

Безопасность и устойчивость

Валидация: Бот проверяет формат ссылок, но корректно обрабатывает пометки «NDA».

Отказоустойчивость: При ошибке обработки данных от нейросети предусмотрен автоматический повторный запрос. Даже если модель выдаст некорректный формат, данные кандидата сохранятся в таблицу.

📋 Команды

/start — Начать интервью.

/restart — Сбросить прогресс и начать заново.

/admin top — Посмотреть статистику и лучших кандидатов (доступно только администраторам).

TalentLens превращает хаотичные отклики в структурированную очередь из сильных инженеров. Мы используем возможности нейросетей для анализа по строгим критериям, а не как генератор случайных мнений.